# DP解

## 问题抽象

> 给定两个字符串s1 和 s2，返回 使两个字符串相等所需删除字符的 ASCII 值的最小和 。

1. 定义f(i,j)，表示取s1[0,i] s2[0,j]的最小删除字符ASCII值的和。
2. f(i,j) = min(f(i-1,j), f(i, j-1), f(j-1, i-1)+(s1(i), s1(j)))
...

不对，这样好像挺难写转移方程的，还要考虑s1[i]==s2[j]的情况

全链的总和值是知道的

那其实问的是留下的最多的是多少

所以定义dp[i][j] 表示留下第i,j个字符以后的ascii值。

